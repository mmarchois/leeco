{% extends 'layout.html.twig' %}

{% block title %}
    {{ 'event.edit.title'|trans({ '%title%': event.title }) }} - {{ parent() }}
{% endblock %}

{% block main %}
    <h1>{{ 'event.edit.title'|trans({ '%title%': event.title }) }}</h1>
    {{ form(form) }}
{% endblock main %}
