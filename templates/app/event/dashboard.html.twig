{% extends 'layout.html.twig' %}

{% block title %}
    {{ event.title }} - {{ parent() }}
{% endblock %}

{% block main %}
    <h1>{{ event.title }}</h1>
     <a href="{{ path('app_events_edit', { uuid: event.uuid }) }}">
        {{ 'common.edit'|trans }}
    </a>
{% endblock main %}
